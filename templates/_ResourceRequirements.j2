{#  ## claims: (ContainerPort [array]) #}
{% if ResourceRequirements.claims is defined %}
claims:
{%   for ResourceClaim in ResourceRequirements.claims | default([], true) %}
{%     filter indent(width=4, first=false) %}
  - {%   include '_ResourceClaim.j2' %}
{%     endfilter %}
{%   endfor %}
{% endif -%}

{#  ## limits: (object) #}
{% if ResourceRequirements.limits is defined %}
limits:
{%   for key in ResourceRequirements.limits | default('') %}
  {{ key }}: {{ ResourceRequirements.limits[key] }}
{%   endfor %}
{% endif -%}

{#  ## requests: (object) #}
{% if ResourceRequirements.requests is defined %}
requests:
{%   for key in ResourceRequirements.requests | default('') %}
  {{ key }}: {{ ResourceRequirements.requests[key] }}
{%   endfor %}
{% endif %}