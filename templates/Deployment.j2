---
#
# Deployment v1 apps  (WORKLOADS APIS)
#
# API Group: WORKLOADS APIS
# Group: apps
# Version: v1
# Kind: Deployment

apiVersion: v1
kind: Deployment
{# ## metadata: (ObjectMeta) #}
{% if resource.metadata is defined %}
metadata:
{%   filter indent(width=2, first=true) %}
{%     set ObjectMeta = resource.metadata %}
{%     include '_ObjectMeta.j2' %}
{%   endfilter %}
{% endif -%}

{# ## Spec: (DeploymentSpec) #}
{% if resource.spec is defined %}
spec:
{%   filter indent(width=2, first=true) %}
{%     set PodSpec = resource.spec %}
{%     include '_PodSpec.j2' %}
{%   endfilter %}
{% endif -%}

{# ## status: (DeploymentStatus) #}
{% if resource.status is defined %}
status:
{%   filter indent(width=2, first=true) %}
{%     set PodStatus = resource.status %}
{%     include '_PodStatus.j2' %}
{%   endfilter %}
{% endif %}

...
